type Mutation {
    createGreeting(
        message: String!
    ): Greeting!

    updateGreeting(
        greetingInput: GreetingInput!
    ): Greeting!

    deleteGreeting(
        id: BigInteger!
    ): String!
}
